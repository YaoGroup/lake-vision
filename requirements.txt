# Deep Learning Framework
torch>=2.0.0
torchvision>=0.15.0

# Scientific Computing
numpy>=1.24.0

# Data Handling
xarray>=2023.1.0
h5py>=3.8.0
pandas>=2.0.0
# Upper bounds are for Sherlock, whose compute nodes are glibc 2.17 and can only
# install manylinux2014 / manylinux_2_17 wheels. netCDF4 >=1.7.3 publishes no
# cp312 x86_64 linux wheel; blosc2 >=4.8 publishes manylinux_2_28 only. Past
# those bounds pip source-builds, which drags in numpy>=2.1 (breaking torch
# 2.2.1) and needs GCC >=10.3 (the module stack provides 10.1.0).
# The lower bound on blosc2 is the CParams API used by build_cache.py.
netCDF4>=1.6.0,<1.7.3  # lakevision/data/datasets.py reads stacks directly
blosc2>=3.3,<4.8       # blosc2 cache (engine/preprocessing/build_cache.py)
scikit-learn>=1.3.0   # splits + metrics in engine/training

# Geospatial (if you're working with satellite data)
rasterio>=1.3.0

# Optional: For visualization during development
matplotlib>=3.7.0